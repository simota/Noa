# Agent workflow screenshots

These images capture native text views using the synthetic content from
`crates/noa-app/examples/native-text-panels.rs`. They show the prompt composer
and output reader; window chrome and Find controls are outside the captured area.
